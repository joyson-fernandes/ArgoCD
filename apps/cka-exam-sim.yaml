apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cka-exam-sim
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default

  source:
    repoURL: https://github.com/joyson-fernandes/ArgoCD.git
    targetRevision: HEAD
    path: manifests/cka-exam-sim

  destination:
    server: https://kubernetes.default.svc
    namespace: cka-exam-sim

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

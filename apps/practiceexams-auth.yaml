apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: practiceexams-auth
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default

  source:
    repoURL: https://github.com/joyson-fernandes/ArgoCD.git
    targetRevision: HEAD
    path: manifests/practiceexams-auth

  destination:
    server: https://kubernetes.default.svc
    namespace: practiceexams-auth

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

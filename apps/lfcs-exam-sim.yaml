apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lfcs-exam-sim
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default

  source:
    repoURL: https://github.com/joyson-fernandes/ArgoCD.git
    targetRevision: HEAD
    path: manifests/lfcs-exam-sim

  destination:
    server: https://kubernetes.default.svc
    namespace: lfcs-exam-sim

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: viewlink
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default

  source:
    repoURL: https://github.com/joyson-fernandes/ArgoCD.git
    targetRevision: HEAD
    path: manifests/viewlink

  destination:
    server: https://kubernetes.default.svc
    namespace: viewlink

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

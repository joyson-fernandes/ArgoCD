apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

configMapGenerator:
  - name: kyverno-dashboard
    files:
      - dashboards/kyverno-dashboard.json

generatorOptions:
  labels:
    grafana_dashboard: "1"
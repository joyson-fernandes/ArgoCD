apiVersion: v1
kind: Namespace
metadata:
  name: ica-exam-sim
  labels:
    app: ica-exam-sim

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ica-exam-sim
  namespace: ica-exam-sim

---
# Deliberately broad, but not cluster-admin: this app dynamically creates
# a namespace per exam session with an unpredictable name, so per-namespace
# Roles can't be pre-scoped — it needs these resource kinds cluster-wide
# to install the vcluster Helm chart's objects, seed the exam state, and
# terminal-exec into the resulting bastion pod. It does NOT get any
# rights over Istio/networking CRDs on the host — that install only ever
# happens against a vcluster's own virtual API server.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ica-exam-sim
rules:
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: [""]
    resources: ["pods", "pods/exec", "pods/log", "pods/portforward", "services", "configmaps", "secrets", "serviceaccounts", "persistentvolumeclaims", "events"]
    verbs: ["*"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "replicasets"]
    verbs: ["*"]
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["roles", "rolebindings", "clusterroles", "clusterrolebindings"]
    verbs: ["*"]
  - apiGroups: ["policy"]
    resources: ["poddisruptionbudgets"]
    verbs: ["*"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["scheduling.k8s.io"]
    resources: ["priorityclasses"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ica-exam-sim
subjects:
  - kind: ServiceAccount
    name: ica-exam-sim
    namespace: ica-exam-sim
roleRef:
  kind: ClusterRole
  name: ica-exam-sim
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ica-exam-sim
  namespace: ica-exam-sim
  labels:
    app: ica-exam-sim
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ica-exam-sim
  template:
    metadata:
      labels:
        app: ica-exam-sim
    spec:
      serviceAccountName: ica-exam-sim
      imagePullSecrets:
        - name: ica-exam-sim-pull-secret
      containers:
        - name: server
          image: ghcr.io/joyson-fernandes/ica-exam-sim:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - secretRef:
                name: ica-exam-sim-ghcr
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: ica-exam-sim
  namespace: ica-exam-sim
  labels:
    app: ica-exam-sim
spec:
  type: ClusterIP
  selector:
    app: ica-exam-sim
  ports:
    - name: http
      port: 80
      targetPort: http

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ica-exam-joysontech-com
  namespace: ica-exam-sim
spec:
  secretName: ica-exam-joysontech-com-tls
  issuerRef:
    name: letsencrypt-dns01-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - ica-exam.joysontech.com

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: internal-only
  namespace: ica-exam-sim
spec:
  ipAllowList:
    sourceRange:
      - 10.0.0.0/8

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ica-exam-sim
  namespace: ica-exam-sim
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`ica-exam.joysontech.com`)
      middlewares:
        - name: internal-only
      services:
        - name: ica-exam-sim
          port: 80
  tls:
    secretName: ica-exam-joysontech-com-tls

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
  namespace: ica-exam-sim
spec:
  redirectScheme:
    scheme: https
    permanent: true

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ica-exam-sim-redirect
  namespace: ica-exam-sim
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`ica-exam.joysontech.com`)
      middlewares:
        - name: internal-only
        - name: redirect-https
      services:
        - name: ica-exam-sim
          port: 80

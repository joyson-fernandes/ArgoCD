apiVersion: v1
kind: Namespace
metadata:
  name: cka-exam-sim
  labels:
    app: cka-exam-sim

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cka-exam-sim
  namespace: cka-exam-sim

---
# Deliberately broad, but not cluster-admin: this app dynamically creates
# a namespace per exam session with an unpredictable name, so per-namespace
# Roles can't be pre-scoped — it needs these resource kinds cluster-wide
# to install the vcluster Helm chart's objects, seed the exam state, and
# terminal-exec into the resulting bastion pod. It does NOT get any
# rights over Istio/networking CRDs on the host — that install only ever
# happens against a vcluster's own virtual API server.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cka-exam-sim
rules:
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: [""]
    resources: ["pods", "pods/exec", "pods/log", "pods/portforward", "services", "configmaps", "secrets", "serviceaccounts", "persistentvolumeclaims", "events"]
    verbs: ["*"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "replicasets"]
    verbs: ["*"]
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["roles", "rolebindings", "clusterroles", "clusterrolebindings"]
    verbs: ["*"]
  - apiGroups: ["policy"]
    resources: ["poddisruptionbudgets"]
    verbs: ["*"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["scheduling.k8s.io"]
    resources: ["priorityclasses"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cka-exam-sim
subjects:
  - kind: ServiceAccount
    name: cka-exam-sim
    namespace: cka-exam-sim
roleRef:
  kind: ClusterRole
  name: cka-exam-sim
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cka-exam-sim
  namespace: cka-exam-sim
  labels:
    app: cka-exam-sim
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: cka-exam-sim
  template:
    metadata:
      labels:
        app: cka-exam-sim
    spec:
      serviceAccountName: cka-exam-sim
      imagePullSecrets:
        - name: cka-exam-sim-pull-secret
      containers:
        - name: server
          image: ghcr.io/joyson-fernandes/cka-exam-sim:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - secretRef:
                name: cka-exam-sim-ghcr
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: cka-exam-sim
  namespace: cka-exam-sim
  labels:
    app: cka-exam-sim
spec:
  type: ClusterIP
  selector:
    app: cka-exam-sim
  ports:
    - name: http
      port: 80
      targetPort: http

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cka-exam-joysontech-com
  namespace: cka-exam-sim
spec:
  secretName: cka-exam-joysontech-com-tls
  issuerRef:
    name: letsencrypt-dns01-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - cka-exam.joysontech.com

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: internal-only
  namespace: cka-exam-sim
spec:
  ipAllowList:
    sourceRange:
      - 10.0.0.0/8

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: cka-exam-sim
  namespace: cka-exam-sim
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`cka-exam.joysontech.com`)
      middlewares:
        - name: internal-only
      services:
        - name: cka-exam-sim
          port: 80
  tls:
    secretName: cka-exam-joysontech-com-tls

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-https
  namespace: cka-exam-sim
spec:
  redirectScheme:
    scheme: https
    permanent: true

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: cka-exam-sim-redirect
  namespace: cka-exam-sim
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`cka-exam.joysontech.com`)
      middlewares:
        - name: internal-only
        - name: redirect-https
      services:
        - name: cka-exam-sim
          port: 80

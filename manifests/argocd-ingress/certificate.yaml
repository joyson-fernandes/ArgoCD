apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-joysontech-com
  namespace: argocd
spec:
  secretName: argocd-joysontech-com-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - argocd.joysontech.com
